# Mocca workspace

This project starts as an engineering workspace, not an application starter.
Define the project before selecting its implementation technology.

The current stage is recorded in `docs/discovery.md`, then technology and
architecture decisions become the basis for implementation specifications.
`docs/integrations.md` records capability choices without making tools hard
dependencies.
