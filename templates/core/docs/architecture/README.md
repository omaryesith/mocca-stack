# Architecture

Describe the selected system shape only after technology direction is decided.
Put lasting cross-cutting decisions in `adr/`; keep implementation-specific
acceptance criteria in `../../specs/`.
