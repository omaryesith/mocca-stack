# Technology selection

List viable technical options only after discovery is sufficiently clear.
Evaluate each against requirements and constraints, then record the selected
direction and rationale in an ADR. Before `IMPLEMENTATION`, inspect local
Profiles whose `matches` tokens fit that approved direction and offer them as
explicit options; never apply one automatically. If a compatible Profile is
rejected, clarify and record how the stack will instead be materialized before
implementation.
