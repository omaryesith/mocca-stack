schema_version: 1
name: conventions-rollback
version: 1.0.0
description: Test Profile that fails during materialization.
capabilities:
  - test:rollback
lifecycle:
  apply_after: technology_selection
  apply_in: IMPLEMENTATION_READY
contributions:
  environment: environment/
  conventions: conventions/
